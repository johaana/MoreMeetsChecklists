/**
 * @fileOverview Redundant sitemap path neutralized to satisfy Next.js metadata collector.
 * The primary sitemap is located at app/sitemap.ts.
 */

export default function sitemap() {
  return [];
}
